BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[PasswordResetCodes] ADD [is_valid] BIT NOT NULL CONSTRAINT [PasswordResetCodes_is_valid_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
