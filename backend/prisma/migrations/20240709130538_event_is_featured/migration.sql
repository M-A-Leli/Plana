BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Events] ADD [is_featured] BIT NOT NULL CONSTRAINT [Events_is_featured_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
