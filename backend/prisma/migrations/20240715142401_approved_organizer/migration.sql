BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Organizers] ADD [approved] BIT NOT NULL CONSTRAINT [Organizers_approved_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
